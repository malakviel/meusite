name: VPN
on:
    push:
        branches: [ main ]

    workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Extrair
      run: tar -xvzf linux_phat_client.tar
      
    - name: instalar
      run: ls
      

  
