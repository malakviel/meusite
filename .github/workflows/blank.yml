name: VPN
on:
    push:
        branches: [ main ]

    workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: teste
      run: |
        echo 1 - Extrair
            tar -xvzf linux_phat_client.tgz
        echo 2 - Lista
      shell: bash
    - name: sh install_linux_phat_client
      

  
